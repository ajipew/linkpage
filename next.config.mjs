/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add remote hostnames here if you ever load logos/images from external URLs
    remotePatterns: [],
  },
};

export default nextConfig;
